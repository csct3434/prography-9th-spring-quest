package prography.pingpong.initialization.service;

import prography.pingpong.initialization.dto.InitializeCommand;

public interface InitializeService {

    void doService(InitializeCommand command);
}
